# Minimal install for running the CSBG research core + evaluation + tests.
# No torch, no speechbrain, no whisper. Installs in seconds.
#   pip install -r requirements-core.txt && pytest
#
# The API is included because it runs -- in degraded mode -- without any of the
# heavy models: it serves the corpus, draws graphs, and reports which branches
# it cannot measure. `tests/test_api.py` covers exactly that configuration.
numpy>=1.26,<2.2
scipy>=1.11
pydantic>=2.6,<3
pydantic-settings>=2.2
networkx>=3.2
rdflib>=7.0
python-dotenv>=1.0
scikit-learn>=1.4

# ---- Figures ----
# Here rather than in the heavy tier: the paper's figures come out of eval/,
# which is core, and matplotlib installs in seconds without torch. Imported
# only inside kavach.eval.figures, so nothing else pays for it.
matplotlib>=3.8

# ---- API ----
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
soundfile>=0.12

# ---- Dev ----
pytest>=8.1
pytest-cov>=5.0
httpx>=0.27
