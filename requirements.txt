# ---------------------------------------------------------------------------
# KAVACH backend dependencies
#
# Split into tiers so the research core (CSBG maths + evaluation) can be
# installed and tested WITHOUT torch/speechbrain/whisper. Heavy models are
# lazily imported; `pip install -r requirements-core.txt` is enough to run
# the full test suite.
# ---------------------------------------------------------------------------

# ---- Core (no ML deps, always required) ----
numpy>=1.26,<2.2
scipy>=1.11
pydantic>=2.6,<3
pydantic-settings>=2.2
networkx>=3.2
rdflib>=7.0
python-dotenv>=1.0

# ---- API ----
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
aiofiles>=23.2

# ---- Speech / ML (heavy; lazily imported) ----
torch>=2.2
torchaudio>=2.2
speechbrain>=1.0
faster-whisper>=1.0
soundfile>=0.12
librosa>=0.10

# ---- Text / multilingual ----
sentence-transformers>=2.7
indic-transliteration>=2.3
regex>=2024.4

# ---- LLM (word-level LID, semantic tagging, challenge generation) ----
anthropic>=0.49

# ---- Evaluation ----
scikit-learn>=1.4
matplotlib>=3.8
pandas>=2.2

# ---- Dev ----
pytest>=8.1
pytest-cov>=5.0
ruff>=0.4
httpx>=0.27
